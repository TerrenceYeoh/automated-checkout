defaults:
  - model: yolo11m
  - training: detect
  - inference: default
  - evaluation: default
  - data: checkout
  - mlflow: default
  - _self_

hydra:
  output_subdir: null       # Don't create .hydra/ config snapshot dir
  run:
    dir: logs/              # Keep Hydra logs out of project root
  job:
    chdir: false            # Don't chdir into the output dir

# Global settings
device: 0
seed: 42
project_root: ${hydra:runtime.cwd}
