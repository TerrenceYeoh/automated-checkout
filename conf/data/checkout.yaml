# Dataset paths (relative to project root)
label_file: data/label.txt
train_dir: data/train
seg_dir: data/segmentation_labels
video_dir: data/testA
crops_dir: data/crops
backgrounds_dir: data/backgrounds

# Scene composition
num_scenes: 30000
min_products: 1
max_products: 8
scene_size: [640, 640]
val_ratio: 0.15
test_ratio: 0.10

# Output
dataset_dir: datasets/checkout
output_dir: outputs
