# Evaluation settings
mode: detection       # "detection", "counting", or "both"
split: val            # val or test
conf_thresh: 0.3
iou_thresh: 0.45

# Counting evaluation (requires ground truth)
ground_truth_dir: null
predictions_dir: null

# Output
save_results: true
output_dir: outputs/eval
